### Funciones

```
// Sincronas

- < nombre >(< parametros >) -> < tipo > {
    ·> // return
}

- Saludar(nombre) -> string {
    ·> `Hola $nombre`
}

// Asíncronas

- < nombre >(< parametros >) => < Promise > {

    ·> // return
}

```


```
// Sincronas

-> < nombre >(< parametros >) < tipo > {
    ·> // return
}

-> Saludar(nombre) string {
    ·> `Hola $nombre`
}

// Asíncronas

=> < nombre >(< parametros >) < Promise > {

    ·> // return
}

```